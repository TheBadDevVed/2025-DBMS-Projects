Here is your schema in a table format:

| Table Name       | Schema Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
|------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **users**        | `CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, phone TEXT UNIQUE, legacyPoints INTEGER DEFAULT 0, password TEXT)`                                                                                                                                                                                                                                                                                                                                     |
| **restaurants**  | `CREATE TABLE restaurants (id INTEGER PRIMARY KEY, name TEXT, cuisine_type TEXT, address TEXT, phone_number TEXT, email TEXT, image TEXT, description TEXT, opening_hours TEXT, password TEXT)`                                                                                                                                                                                                                                                                |
| **dishes**       | `CREATE TABLE dishes (id INTEGER PRIMARY KEY, name TEXT, description TEXT, price REAL, prep_time_minutes INTEGER, cook_time_minutes INTEGER, course TEXT, dietary_restrictions TEXT, spiciness_level INTEGER)`                                                                                                                                                                                                                                                 |
| **ingredients**  | `CREATE TABLE ingredients (id INTEGER PRIMARY KEY, name TEXT UNIQUE, allergen TEXT, calories REAL, protein_grams REAL, fats_grams REAL, carbs_grams REAL)`                                                                                                                                                                                                                                                             |
| **dish_ingredients** | `CREATE TABLE dish_ingredients (dish_id INTEGER, ingredient_id INTEGER, quantity REAL, unit TEXT, PRIMARY KEY(dish_id, ingredient_id), FOREIGN KEY(dish_id) REFERENCES dishes(id), FOREIGN KEY(ingredient_id) REFERENCES ingredients(id))`                                                                                                                                                                         |
| **menus**        | `CREATE TABLE menus (id INTEGER PRIMARY KEY, restaurant_id INTEGER, name TEXT, description TEXT, FOREIGN KEY(restaurant_id) REFERENCES restaurants(id))`                                                                                                                                                                                                                                                               |
| **menu_dishes**  | `CREATE TABLE menu_dishes (menu_id INTEGER, dish_id INTEGER, PRIMARY KEY(menu_id, dish_id), FOREIGN KEY(menu_id) REFERENCES menus(id), FOREIGN KEY(dish_id) REFERENCES dishes(id))`                                                                                                                                                                                                                                    |
| **tables**       | `CREATE TABLE tables (id INTEGER PRIMARY KEY, restaurant_id INTEGER, table_number TEXT, capacity INTEGER, status TEXT, FOREIGN KEY(restaurant_id) REFERENCES restaurants(id))`                                                                                                                                                                                                                                         |
| **orders**       | `CREATE TABLE orders (id INTEGER PRIMARY KEY, user_id INTEGER, restaurant_id INTEGER, table_id INTEGER, status TEXT, total_amount REAL, special_notes TEXT, created_at TEXT DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(user_id) REFERENCES users(id), FOREIGN KEY(restaurant_id) REFERENCES restaurants(id), FOREIGN KEY(table_id) REFERENCES tables(id))`                                                                  |
| **order_items**  | `CREATE TABLE order_items (order_id INTEGER, dish_id INTEGER, quantity INTEGER, FOREIGN KEY(order_id) REFERENCES orders(id), FOREIGN KEY(dish_id) REFERENCES dishes(id))`                                                                                                                                                                                                                                               |
